services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-kernel
    ports:
      - "9500:9500"
    volumes:
      - ./database:/app/database
    restart: unless-stopped
