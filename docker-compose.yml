services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-web
    ports:
      - "9503:9503"
    volumes:
      - ./data:/app/data
      - ./bases:/app/bases
    restart: unless-stopped
