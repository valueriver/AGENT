services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-kernel
    ports:
      - "9500:9500"
    volumes:
      - ./src/database:/app/src/database
    restart: unless-stopped
